@model DersEkleModel

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm">
                <div class="card-body p-4">
                    <h3 class="mb-4 text-center">📚 Ders Ekle</h3>

                    <form asp-action="DersEkle" method="post" novalidate>
                        <div class="mb-3">
                            <label asp-for="Ad" class="form-label">Ders Adı</label>
                            <input asp-for="Ad" class="form-control" placeholder="Örn: Programlama Dilleri" />
                            <span asp-validation-for="Ad" class="text-danger small"></span>
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-save"></i> Kaydet
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
