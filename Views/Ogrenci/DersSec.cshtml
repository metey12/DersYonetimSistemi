@model OgrenciDersSecimModel

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-body p-4">
                    <h3 class="mb-4 text-center text-primary">📘 Ders Seçimi</h3>

                    <form asp-action="DersSec" method="post">
                        <input type="hidden" asp-for="OgrenciId" />

                        <div class="mb-4">
                            @for (int i = 0; i < Model.Dersler.Count; i++)
                            {
                                <div class="form-check mb-2">
                                    <input asp-for="Dersler[i].Secili" class="form-check-input" id="ders_@i" />
                                    <label class="form-check-label" for="ders_@i">
                                        @Model.Dersler[i].DersAdi
                                    </label>

                                    <input type="hidden" asp-for="Dersler[i].DersId" />
                                    <input type="hidden" asp-for="Dersler[i].DersAdi" />
                                </div>
                            }
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-check2-square"></i> Kaydet
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
